# 0.6.1fix1 multiplayer merge

Sources: local checkpoint `e7b36e7e0`; official `aa1575c2e` (`upstream/main`).

## Integration choices

- Retain the server budgeted settlement coordinator, isolated settlement date view, player barrier, incremental network updates, and temporary chunk ownership tracking.
- Adopt official native mine layouts and monsters. Port local population limits, delayed native entity insertion and the Youer damage/spawn bridge. Retry rejected insertions once, scoped to the current floor data identity; do not refill cleared floors on reentry.
- Adopt official fish pond prefab and husbandry lifecycle. Retain pond-by-pond daily work, deterministic daily randomness, and chunk-tracking color deltas for creation, relocation and removal.
- Run crop growth as budgeted entries, then stable giant-crop anchors, then farmland maintenance. Retain explicit historical dates, day idempotency, paddy watering and official soil restoration. Offline catch-up follows the same growth-before-giants order.
- Move official livestock settlement into home/day work items, preserving its durable batch journal. Pause background catch-up while overnight settlement owns the world. Construction participates before world crop/livestock stages.
- Generate artifacts by one marker/candidate per scheduler item with dated zone checkpoints. Startup generation is budgeted. Quarry initialization uses the official finite attempt count spread across ticks.
- Delegate farm cave migration and loading to the official versioned runtime; never replay the retired cave schematic. Resolve the current farm instance in queued callbacks.
- Guard new server gameplay payloads during settlement; retain ungated acknowledgements and cancellation. Include production JSON in registry packet size accounting.
- Make model compiler Python selection and UTF-8 mode work on Windows. Test heap defaults to 2 GiB, configurable using `-PtestHeapSize`.

## Validation and limits

- Before merge: original main project tests and ItemsAdder compatibility plugin build passed.
- After integration: `gradlew.bat test build --console=plain` passed; 606 tests, zero failures/errors/skips.
- Review was limited to resolved conflicts, changed integration points and directly related multiplayer behavior, not a repository-wide audit.
- No multiplayer live-server TPS profiling was performed. Home/day livestock transactions and individual building projection operations remain atomic; large individual buildings may still exceed a tick budget. Artifact candidate-pool construction still materializes the region column set, although world probes are budgeted.
- This merge is local; no remote push or server deployment is included.
