# 0.6.1fix2 multiplayer merge

Sources: local `333ba4217`; official `121b98630` (`upstream/main`).

## Integration choices

- Keep budgeted daily settlement, frozen farm/date snapshots, chunk leases and player access guards.
- Apply official twig ecology, protected floor/occupied-tile placement and wild-tree farm boundaries inside the existing incremental debris, pasture and wild-seed work units. Retain deterministic daily randomness and historical-date processing.
- Adopt infertile soil growth/persistence, building fixes, shipping ownership/animation, trash-can support and official resources. Preserve existing crop/giant ordering and offline catch-up architecture.
- Flush mini shipping bins in the existing PREPARE shipping stage before player settlement and non-participant cleanup; do not restore the upstream synchronous day loop.
- Keep the Flashback missing-dimension login guard before persistent player initialization and retain local settlement login handling.
- Retain local loaded-NPC lookup alongside upstream collision/path diagnostics. The new construction progress payload is clientbound; existing serverbound settlement guards remain intact.
- Add `TrashCanLevel` to the optimized client sync allowlist. The existing client-field coverage test caught the missing field after the merge; without it upgraded trash cans would render as basic on clients.

## Validation scope

- `gradlew.bat test build --console=plain` passed: 606 tests, zero failures/errors/skips. Artifact: `build/libs/stardewcraft-0.6.1fix2.jar` (219110405 bytes).
- The settlement lifecycle contract now requires both mini and regular shipping bins in the PREPARE flush.

Review is limited to merge conflicts and affected integration points, not a repository-wide audit. No live multiplayer TPS profiling or server deployment is included. Existing atomic operations, including shipping flushes and building updates, remain atomic.

Full staged whitespace inspection reports pre-existing upstream whitespace; integration conflict edits are checked separately. No unrelated formatting sweep is included.
