package com.stardew.craft.fishing;

/** Cosmetic state; never used to decide catches, consume attachments, or award loot. */
public enum FishingPresentationPhase { STOP, CHARGE, CAST, BITE, HOOK, MINIGAME, CATCH, FAIL, RETRIEVE }
